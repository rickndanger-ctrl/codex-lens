# Codex Lens Build Log

## 2026-08-27 — background rollover audio overlap repair

- A fresh locked/background audit contradicted the older rollover result on the latest source: a replacement peer completed signaling and session setup but reported zero outbound microphone packets, after which iOS suspended the still-present app process.
- Rollover now keeps the already-proven Ray-Ban audio peer alive until the replacement reports a nonzero outbound audio packet count. Bluetooth route changes keep the shared WebRTC audio unit alive whenever either the configured replacement or retained peer exists; media-service reset disables both tracks and fails closed.
- The revision passes 80/80 Swift tests and a signed generic-iOS Xcode build. Reinstallation and repeated locked rollover proof remain pending because the iPhone became unavailable to Xcode after the preceding install.

## 2026-08-27 — premature voice interruption repair

- Physical use exposed an interruption defect unrelated to the optional `Codex` attention word: Realtime semantic VAD was configured with high eagerness and could commit the opening words of a request as a complete turn. The assistant then spoke a generic prompt such as “tell me what you need” while the wearer was still asking the question.
- Changed semantic VAD eagerness from `high` to `medium` so brief thinking pauses do not end the wearer turn as aggressively. Added a local fail-silent guard for incomplete request fragments such as `Can you...`, `What do you...`, and `I need you to...`; the silent classifier and main assistant instructions also forbid generic readiness speech for fragments. Complete natural requests still follow the normal no-wake-word path.
- Verification passes: 80/80 `CodexLensKit` tests, signed physical-iPhone Xcode build, strict signature verification, installation sequence 4120, and app launch. Physical acceptance then passed after multiple requests: the assistant completed a time answer, correctly identified the open Mac app, then stayed silent through a deliberate one-second pause after `Can you...` and answered the completed day question correctly. The premature-interruption regression is closed on this install.

## 2026-08-27 — twelve-inch small-print reading pass

- Physical text-reading acceptance exposed a real distance defect: the previous reading profile could read a printed target only at roughly four inches. Lowering Apple Vision's minimum text-height threshold alone still read only the large words at twelve inches, so that intermediate build failed the small-print gate.
- The corrected reading path now requests HEIC from the glasses for fine-edge preservation, decodes it only in memory, runs Apple Vision OCR at full frame plus centered 80% and 60% regions, selects the strongest text evidence, and still sends only a bounded metadata-stripped JPEG. The ordinary fast-photo path remains unchanged on JPEG.
- On the revised signed install, with the iPhone locked, the wearer held the same target twelve inches away and asked `Codex, read this text`. The assistant correctly read the small words, and the wearer confirmed the white camera light turned off afterward. Twelve-inch small-print reading and post-capture shutdown therefore pass physically; longer-distance or unusually tiny text remains limited by Meta's wide 1440x1080 public image.

## 2026-08-26 — function-rich wearable baseline and direct Codex inspection

- Added optional guaranteed attention through the word `Codex`. A turn beginning with `Codex` now bypasses the uncertain ambient classifier, but the word remains optional: natural direct requests still work without it. Visual requests retain deterministic one-tool capture. The change passes 79/79 Swift tests, signed physical-iPhone compilation, strict signature verification, installation, and launch.
- Physical locked-phone acceptance passed all attention branches on that install. `Codex, what time is it?` returned the correct time; the no-name request `What day is it?` returned the correct day; the ambient statement `I went to the store yesterday and bought some coffee` stayed silent.
- The locked request `Codex, take a pic` captured the glasses view, returned a correct description, and the wearer confirmed the white camera light turned off afterward.
- The locked request `Codex, inspect the selected project and tell me what bug is in it` reached the selected Sample Project and identified its `add` function. The wearer believed the spoken answer included the exact subtract-instead-of-add defect; the route and project identification pass physically, while exact spoken wording should be treated as wearer-recalled rather than logged verbatim.
- Kept ordinary `Take a pic` requests on the proven one-frame fast path: one 1440x1080 still, 8.696 seconds of physical capture time, a correct completed description, and the glasses camera light off afterward. Explicit requests to read text, a screen, code, a page, a sign, or a document still use two high-resolution OCR candidates. A two-second HVC1 stream-frame shortcut produced no usable frame and only added delay, so it was removed completely.
- Added an in-flight visual-request guard. A second capture request now reports `already_capturing` instead of opening overlapping Meta SDK work or delaying the first result.
- Removed texting from the active Realtime tool surface and assistant instructions. Siri is the glasses messaging path; dormant message code remains recoverable but Codex Lens will not advertise, prepare, or send texts.
- The iPhone now loads the gateway's authenticated approved-project list, requires at least one allowlisted project, remembers the selected project, and displays its edit and commit/push/deploy policy. The live registry currently exposes only `Sample Project`; the gateway/control-plane repository is deliberately not made remotely self-editable.
- Added authenticated `POST /v1/codex/inspect` and the voice tool `inspect_codex_project`. The phone injects the selected allowlisted project id, while the spoken request supplies only the question. The gateway runs a fresh local Codex app-server turn inside that project's directory with a read-only sandbox, no network access, no approval bypass, structured output, and relative-path validation.
- Live gateway proof against `Sample Project` completed in 22.4 seconds. Codex correctly found that `add` subtracts its right operand and identified `src/calculator.js`, `README.md`, and `test/calculator.js` as relevant files.
- Verification passes under Node 26: gateway typecheck, lint, production build, 53 test files / 450 tests, and live loopback health. The Swift package passes 79/79 tests. A signed physical-iPhone Xcode build, strict installation path, and installation on device succeeded. The newest install still needs one user launch before glasses voice acceptance.

## 2026-08-26 — coach playback investigation and on-demand camera repair

- The server selected `coach_interjection`, but the first serialization/VAD repair did not fix physical playback: the wearer still heard the correction stop before its final date. That failed acceptance is retained rather than treated as a pass.
- Realtime now serializes attention classification and audible follow-ups, queues bounded turns while a response is active, and disables automatic response interruption on the wearer-beamformed HFP route. The remaining hard cap was `max_output_tokens: 100`; because Realtime applies the cap to the full generated response, including audio, the latest build raises the coach-only transport allowance to 512 while retaining the classifier's 25-word content limit. A debug-only client transcript then exercised the production classifier and audio-response path: classification completed with 91 output tokens, selected `coach_interjection`, and the spoken follow-up completed normally with 164 output tokens and no completion reason/error. This proves the old 100-token cap could not contain the actual correction. The wearer confirmed the automatic correction finished, though its ending sounded close to the cutoff. With the observed 164-token response under a 512-token cap, the exact Apollo playback gate passes. The signed build passes 74/74 Swift tests and strict code-sign verification.
- Removed continuous camera pre-arming after the wearer observed the glasses capture light cycling when no photo was requested. Voice remains persistent, while the Meta camera now opens only for an explicit photo/look/read request and the transient stream closes after capture.
- The updated signed build installed and launched on the physical iPhone with `RB Meta 0030` selected for microphone and sound, signaling HTTP `201`, and `session.updated`. Runtime logs show no camera stream start, and the wearer confirmed the glasses camera light is off while the assistant remains live. A locked on-demand photo is still required to prove the privacy fix did not regress hands-free capture.
- A debug-only explicit photo request then proved the closed-camera path in both foreground and true iOS background states. Foreground capture started from no stream, returned one 1440x1080 frame in 11.17 seconds, stopped the camera immediately, and completed the visual answer. For the background run, Settings was launched over Codex Lens, the app logged `UIApplicationDidEnterBackground`, retained voice with the camera closed, then created the camera on demand, returned one 1440x1080 frame in 8.58 seconds with 68 OCR characters, stopped the camera, and completed the visual response. The wearer confirmed the idle camera light was off before this run. Locked-screen on-demand capture and physical confirmation of the post-capture light remain separate gates.
- A live human conversation then exposed the same token-limit class of bug in the quiet-intent classifier: one classification ended `incomplete` at its 180-token cap and therefore failed safely to silence. The classifier allowance is now 512 while its required tool schema, fail-closed behavior, and 25-word interjection limit remain unchanged. The revised normal build passes 74/74 Swift tests, physical-device compilation, strict signature verification, installation, Ray-Ban audio selection, signaling HTTP 201, and `session.updated`; the camera remains closed.
- After the classifier-cap install, the assistant stayed silent through consecutive real technical-conversation turns, including questions judged to be aimed at another person, uncertain pricing and CUDA claims, personal reflections, and an Nvidia opinion. Every observed classification completed normally under the new allowance and selected `silent`; the assistant did not announce, answer, or interrupt. This is a live restraint soak, not proof of unobserved speaker identity or every possible conversation.
- The wearer confirmed the glasses camera light turned off after the background on-demand capture. That closes the physical post-capture idle-light gate: the camera opens transiently for a requested still and shuts down afterward.
- The same live conversation then exposed a false positive: “Can you tell him Redrum?” was treated as a direct assistant request. The local attention gate now fails closed on ambiguous pronoun-only human relay phrases (`tell him/her/them`, `ask him/her/them`, and `let him/her/them know`) while explicit delivery requests such as “Text John on iMessage ...” continue to the assistant. The isolated classifier policy carries the same mandatory-silence rule. The corrected build passes 75/75 Swift tests, physical-device compilation, strict code-sign verification, installation, Ray-Ban audio selection, signaling HTTP 201, and `session.updated`; idle camera pre-arming remains disabled.
- Explicit visual commands are now deterministic instead of leaving tool selection to the voice model: photo/look/read requests produce a response with `capture_glasses_view` forced exactly once. A debug-only request injected through the same local attention gate exercised that production path with “Take a pic”: the forced tool call completed, opened the previously closed Meta session and stream, captured one 1440x1080 reading-profile frame in 11.30 seconds, stopped the camera immediately, queued the image, and completed the visual answer. The app was relaunched normally with no injected request. The final build passes 76/76 Swift tests, signed-device compilation, strict code-sign verification, installation, Ray-Ban audio selection, signaling HTTP 201, and `session.updated`, with no idle stream.
- A physical locked-phone retry exposed a speech-recognition edge: the glasses rendered “Take a pic” as “take a pick,” so the conservative attention gate stayed silent even though the camera was healthy. The production session now biases Realtime transcription toward the literal camera phrase and accepts the narrow `take a pick` homophone locally, while deliberately leaving the ordinary phrase `take your pick` non-visual. The revised build still passes 76/76 Swift tests, signed-device compilation, strict code-sign verification, installation, Ray-Ban audio routing, signaling HTTP 201, and `session.updated`. With device lock state reporting `passcodeRequired: true`, the exact three-word command was classified `explicit visual action`, forced one glasses capture, returned a 1440x1080 reading-profile frame with 29 OCR characters at 1.000 confidence in 7.91 seconds, stopped the camera, and completed the spoken visual response in about 14.12 seconds end to end. The wearer confirmed the description was correct and the white camera light was off afterward. Locked no-tap one-shot capture therefore passes physically on this build.
- A later fold/reopen regression showed why rebuilding the WebRTC peer was unreliable: replacement peers completed signaling but sent zero outbound audio packets, and disabling the underlying WebRTC audio unit allowed iOS to suspend the locked app before the returning HFP route could be observed. The corrected transport retains the configured peer, disables only its outgoing `RTCAudioTrack` when the glasses fold, and keeps the audio unit active as a background keepalive. During the physical locked test, the route temporarily fell back to the iPhone microphone/receiver but `transmit=false`; after reopening, `RB Meta 0030` returned automatically, `transmit=true`, and outbound statistics advanced from 5,147 packets / 259,443 bytes to 5,754 packets / 289,178 bytes without a new peer generation.
- The same locked post-reopen session answered a natural current-time request completely, then accepted `Take a pic` without a wake word or tap. It opened the on-demand Meta camera, returned one 1440x1080 frame in 13.69 seconds, stopped the camera, queued the image, completed the spoken visual response, and the wearer confirmed both the description and that the white camera light was off. Locked fold/reopen voice, on-demand capture, and post-capture camera shutdown therefore pass on the installed build. The 13.69-second still latency remains an open performance defect.

## 2026-08-26 — home-Mac voice handoff contract

- Reframed the Realtime assistant as the hands-free voice and vision front end for the wearer's home Codex/Mac gateway.
- Made direct requests such as “use Codex” and “check my computer” explicitly route to the verified Mac Computer Use bridge.
- Removed the advertised `start_task` and `report_progress` tools because the iPhone had no handlers for them; this prevents the voice model from selecting a tool that could only return `Unsupported tool`.
- Removed prompt instructions for planning/task tools that were not actually exposed to the Realtime session.
- Verified 69/69 Swift tests, a signed physical-iPhone build, and installation on the paired iPhone 17 Pro. Launch is pending one unlock because iOS denied remote launch while the phone was locked.

## 2026-08-26 — Computer Use latency pass

- Confirmed the first glasses-to-home-Mac Computer Use request reached `/v1/computer/use`, returned HTTP 200, and correctly identified ChatGPT as the frontmost Mac app.
- Measured the verified request at 58.5 seconds, which is functionally correct but too slow for a wearable interface.
- Changed isolated, bounded GUI runs from the frontier coding model to the faster `gpt-5.6-luna` model. The model remains configurable with `CODEX_LENS_COMPUTER_MODEL`, and all existing sandbox, tool, confirmation, and result-verification boundaries remain unchanged.
- Verified a second real glasses request remained accurate and completed in 37.3 seconds.
- Rejected and removed a persistent warm app-server prototype: it produced a false Screen Recording warning on its first call and only reduced the next call to 34.7 seconds. The small speed gain did not justify weakening reliability.
- Reverified the stable gateway with Node 26: typecheck passed, lint passed, 447/447 tests passed, and the production build passed.
- Added a dedicated read-only `get_frontmost_mac_app` voice tool and `/v1/computer/frontmost` gateway endpoint for questions that only ask which Mac app is active. The live endpoint returned `ChatGPT` in 0.26 seconds without a screenshot or Computer Use model turn; general Mac operations still use the bounded Computer Use path.
- Verified the new path with the full gateway verification suite, 70/70 Swift package tests, a live authenticated endpoint call, and a signed physical-iPhone build. The build installed and launched on the paired iPhone 17 Pro.
- Physical glasses acceptance passed: the wearer asked which Mac app was open, the Realtime model selected the new tool, the gateway answered in 0.26 seconds, and the wearer confirmed the spoken `ChatGPT` answer was correct and fast.
- Locked-phone physical acceptance also passed for the fast tool: the wearer repeated the natural request without touching the phone and confirmed the spoken `ChatGPT` answer was correct and fast.
- Post-fold camera recovery passed while locked: after closing and reopening the glasses, the wearer asked naturally to take a picture without touching the phone. The glasses capture completed and the assistant gave a detailed, correct description.
- A second consecutive locked request captured a different view and again produced a correct description, proving the camera rearmed after the first still rather than passing only once.
- A third consecutive locked capture also produced the correct answer. The current signed build therefore passed a 3-for-3 post-fold camera re-arm run without unlocking or touching the phone.
- Locked rollover acceptance passed with the iPhone in `UIApplicationDidEnterBackground`: connection generations 2 through 9 completed eight consecutive 20-second renewals with signaling HTTP `201`, ICE connected, an open data channel, `session.created`, `session.updated`, Ray-Ban microphone/speaker routing, and successful camera recovery after every renewal. After the renewals, the no-wake request “What time is it?” was classified as a direct request and the wearer confirmed the spoken answer was correct and included date and time-zone detail. The app was then relaunched without the test override; the fresh production session connected, armed glasses audio and camera, reported thermal state `none`, and emitted no debug rollover schedule.

## 2026-08-26 — Messages account readiness repair

- Diagnosed the live false `ready=false` result: Messages exposed six account records, but three throw AppleEvent error `-10000` when their service type is read. The old scan let those unreadable records hide the valid connected services.
- Hardened Messages readiness, service lookup, and send validation to skip unreadable account records while still requiring an enabled, connected account of the exact wearer-selected service.
- Live authenticated readiness now reports Contacts accessible, one connected iMessage account, one connected SMS account, no connected RCS account, and `ready=true`. No message was sent.
- Full Node 26 verification passed after the repair: typecheck, lint, 447/447 tests, build, restart, and the live 0.28-second readiness check.
- A later live readiness check exposed a separate startup regression: Contacts access returned false whenever the Contacts app was not already running, despite an allowed permission record. The deployed AppleScript now launches Contacts before readiness or name lookup and launches Messages before account inspection or send. With Contacts fully stopped, the rebuilt and restarted gateway launched it and returned `ready=true`, with one iMessage account and one SMS account. Typecheck, lint, and 447/447 tests pass; no message was sent.

## 2026-08-26 — proactive debate correction repair

- Physical Conversation Coach acceptance first passed ordinary-conversation restraint: “I went to the store yesterday and bought some coffee” was classified `silent`, and the wearer confirmed the glasses did not speak.
- The first factual-debate test failed: “No, I’m sure Apollo 11 landed on the Moon in 1972” was classified `silent` because the model incorrectly treated debate interjection as outside direct assistance.
- Replaced the ambiguous classifier wording with a testable shared policy that declares proactive debate and fact-check help a core function, states that no direct request is required, and includes a mandatory Apollo 11 `1972` to `1969` interjection example.
- The revised policy passes 71/71 Swift tests, a signed physical-iPhone build, strict code-sign verification, installation, launch, Ray-Ban audio routing, and camera re-arm. On the repeat physical test, the same Apollo statement was classified `coach_interjection`, and the wearer confirmed the glasses correctly interrupted with the factual correction.
- The wearer confirmed the glasses heard him but not his son at ordinary conversation distance. Meta's public iOS path supplies narrowband HFP audio beamformed toward the wearer rather than raw room or microphone-array audio. The wearer accepted wearer-only correction as the release behavior, so the production input policy returned to `near_field` to optimize his voice instead of degrading it in an unsuccessful attempt to recover distant speech.
- OpenAI's documented diarized Realtime transcription model was tested but the live project returned `Your organization does not have access to this transcription model.` The failing process was terminated immediately. The installed build keeps the supported `gpt-4o-mini-transcribe` model and does not claim speaker identity; speaker-segment handling remains ready for a future entitlement.
- The far-field experiment passed 74/74 Swift tests and heard a loud nearby Mac voice, but the wearer's real test with his son remained unreliable. That lab result is not evidence of ordinary second-speaker capture and is no longer a release gate.
- The wearer-focused `near_field` build then passed 74/74 Swift tests, signed-device build, strict signature verification, install, and launch on the physical iPhone. The live session selected `RB Meta 0030` for microphone and sound, received signaling HTTP `201`, reached `session.updated`, and armed the glasses camera on its first recovery attempt.
- Replaced the lazy quiet-gap cleanup, which waited for another utterance, with a real 90-second state-queue timer. Every completed transcript rearms one bounded reset; an explicit ending, disconnect, or newer transcript cancels the stale timer. The updated signed build installed and launched, selected Ray-Ban audio, reached `session.updated`, and armed the camera on its first attempt. Locked physical timer evidence remains pending.

## Ticket 111 — deterministic text vertical slice

- Added `src/demo/text-vertical-slice.ts`, covering conversation → Engineering Plan → engineering approval → Execution Plan → execution approval → queued Codex task.
- Wired the slice to `npm run demo`; its transcript is stable across runs and needs no secret, network service, or glasses hardware.
- Added `tests/text-vertical-slice.test.ts`, which asserts that the protocol reaches a queued Codex task and that repeated runs produce the same transcript.
- Replaced the older demo command that stopped after Engineering Plan approval. The underlying protocol, gateway, and existing live Codex slice remain unchanged.
- Restated the accurate product status: Codex Lens remains glasses-first; physical glasses integration and on-device end-to-end validation are not complete.
