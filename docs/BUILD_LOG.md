# Codex Lens build log

## 2026-08-30 — controlled recovery and reconnect serialization

- Restored the physical iPhone app to the exact last wearer-proven source boundary from build 4120. The later experimental peer-overlap/audio-probe changes were removed rather than repaired in place.
- The voice regression was an actor-reentrancy race: overlapping session starts could enter the same transport connection concurrently, and the stale attempt could then disconnect the newer peer. Realtime transport establishment and stale-peer cleanup are now serialized. Deterministic tests cover two overlapping starts, stop during a queued start, and a burst of twenty replacement starts; only the final generation can connect.
- The full CodexLensKit suite passes 83/83. The physical-device app builds, passes strict code-sign verification, installed as database sequence 4816, and launched as a live iPhone process.
- A direct startup console capture established the current external state without asking the wearer to cycle the glasses: Meta device `0030` was disconnected and no Ray-Ban HFP input was available. The app correctly armed passive recovery and did not fall back to the iPhone microphone. This is waiting-for-accessory state, not an app crash; current mic-to-spoken-answer acceptance remains unproved until the accessory reconnects naturally.
- The no-wake attention gate now also fails silent for incomplete polite fragments such as “Can you please…”, “Could you go ahead and…”, and “I was wondering if you could…”, including optional `Codex` addressing. Complete natural requests and the optional `Codex` recovery address remain enabled; `Lens` is not required.
- Local and tailnet-authenticated Messages readiness both return Contacts accessible, one connected iMessage account, one connected SMS account, no RCS account, and `ready=true`. No message was prepared or sent.

## 2026-08-27 — background rollover audio overlap repair

- A fresh locked/background audit contradicted the older rollover result on the latest source: a replacement peer completed signaling and session setup but reported zero outbound microphone packets, after which iOS suspended the still-present app process.
- Rollover now keeps the already-proven Ray-Ban audio peer alive until the replacement reports a nonzero outbound audio packet count. Bluetooth route changes keep the shared WebRTC audio unit alive whenever either the configured replacement or retained peer exists; media-service reset disables both tracks and fails closed.
- The revision passes 80/80 Swift tests and a signed generic-iOS Xcode build. Reinstallation and repeated locked rollover proof remain pending because the iPhone became unavailable to Xcode after the preceding install.

## 2026-08-27 — premature voice interruption repair

- Physical use exposed an interruption defect unrelated to the optional `Codex` attention word: Realtime semantic VAD was configured with high eagerness and could commit the opening words of a request as a complete turn. The assistant then spoke a generic prompt such as “tell me what you need” while the wearer was still asking the question.
- Changed semantic VAD eagerness from `high` to `medium` so brief thinking pauses do not end the wearer turn as aggressively. Added a local fail-silent guard for incomplete request fragments such as `Can you...`, `What do you...`, and `I need you to...`; the silent classifier and main assistant instructions also forbid generic readiness speech for fragments. Complete natural requests still follow the normal no-wake-word path.
- Verification passes: 80/80 `CodexLensKit` tests, signed physical-iPhone Xcode build, strict signature verification, installation sequence 4120, and app launch. Physical acceptance then passed after multiple requests: the assistant completed a time answer, correctly identified the open Mac app, then stayed silent through a deliberate one-second pause after `Can you...` and answered the completed day question correctly. The premature-interruption regression is closed on this install.

## 2026-08-27 — latest installed small-text reading build

- The original reading profile required approximately four inches to read small print. A first Apple Vision minimum-text-height adjustment still read only large words at twelve inches and failed physical acceptance.
- Reading mode now requests HEIC for better fine-edge preservation, then performs Apple Vision OCR on the full frame plus centered 80% and 60% regions before selecting the strongest evidence. The image remains in memory and leaves the phone only as the existing bounded metadata-stripped JPEG; fast ordinary photos stay on the proven JPEG path.
- Locked physical acceptance passes on the revised install: the same target at twelve inches produced a correct reading of the small words, and the white camera light turned off afterward. Text beyond that scale remains subject to the public 1440x1080 Meta image limit.

## 2026-08-26 — latest installed function-rich build

- `Codex` is now an optional guaranteed-attention word rather than a requirement. It bypasses the ambiguous ambient classifier, while clear natural requests still work without a name. The signed install passes 79/79 Swift tests and strict signature verification.
- Locked physical acceptance passed: `Codex, what time is it?` answered correctly; `What day is it?` answered correctly without `Codex`; ordinary coffee conversation stayed silent; `Codex, take a pic` returned the correct view and left the white camera light off.
- A locked direct-repository request reached the selected Sample Project and identified the calculator's `add` function. The wearer believed the answer also described its subtract-instead-of-add defect, but did not retain the exact wording; route/project proof passes and verbatim answer proof remains distinct.
- Ordinary `Take a pic` uses one fast 1440x1080 still. The physical baseline completed capture in 8.696 seconds, produced a correct completed description, and left the glasses camera light off. Text/screen/code/page/sign/document requests retain two high-resolution OCR candidates. A failed two-second HVC1 stream-frame shortcut returned no usable frame and increased latency, so it was removed rather than retained as a fallback.
- Concurrent visual requests fail fast with `already_capturing`, preventing overlapping Meta SDK camera work.
- Siri remains the messaging path through the glasses. Codex Lens no longer advertises or exposes its dormant text preparation and sending tools to the voice model.
- The app now authenticates through `/v1/projects`, loads the gateway allowlist, remembers the selected approved project, and shows its edit and commit/push/deploy policy. Only `Sample Project` is presently registered; the Codex Lens control-plane repository is intentionally not a remote self-edit target.
- The active voice tool `inspect_codex_project` calls the new authenticated `/v1/codex/inspect` route. The phone supplies the selected allowlisted project id. The Mac starts a local Codex app-server turn in that exact project with a read-only sandbox, network disabled, structured JSON output, and validated relative file paths.
- A live inspection of `Sample Project` completed in 22.4 seconds and correctly reported that the calculator's addition function subtracts the right operand. It returned `src/calculator.js`, `README.md`, and `test/calculator.js` as relevant files.
- Node 26 verification passes: typecheck, lint, gateway build, 53 test files / 450 tests, and live gateway health. The Swift package passes 79/79 tests. The signed physical-iPhone build and installation pass. Launch the newest install once, then physically verify the direct Codex question and the locked one-shot camera regression after the glasses are fully charged.

## 2026-08-26 — latest installed on-demand-camera build

- The previous background-camera design kept a two-frame-per-second high-resolution stream open for the entire voice session. The wearer correctly identified the cycling capture light; that was real continuous camera activity, not an idle indicator.
- The latest build keeps glasses voice active but does not pre-arm the camera. An explicit photo/look/read tool call creates a transient Meta camera stream, captures the still, and closes the stream afterward.
- The signed physical-device build succeeds, strict code-sign verification passes, and 74/74 Swift package tests pass. After installation, the live app selected the Ray-Ban microphone and speaker, received signaling HTTP `201` and `session.updated`, emitted no camera-start event, and the wearer confirmed the glasses light remained off.
- A foreground one-shot request then started the previously closed camera, captured one 1440x1080 frame in 11.17 seconds, stopped the camera immediately, and completed the visual answer. A separate automated background run launched Settings over Codex Lens and observed `UIApplicationDidEnterBackground`; with voice retained and the camera still closed, the request started the Meta stream, delivered one 1440x1080 frame in 8.58 seconds with 68 OCR characters, stopped the camera immediately, and completed the model response. This proves on-demand capture while backgrounded, but not yet with the iPhone screen locked.
- During a later live conversation, one quiet-intent classification reached its separate 180-token maximum and ended `incomplete`, correctly defaulting to silence but potentially suppressing useful help. The installed normal build raises classifier transport headroom to 512 while retaining the forced tool schema, fail-closed handling, and 25-word speech policy. It passes 74/74 Swift tests, signed device build, strict signature verification, install, Ray-Ban audio routing, signaling HTTP 201, and `session.updated` with no idle camera stream.
- In the live conversation immediately after that install, consecutive technical statements and questions were all classified `silent`, including turns judged to be aimed at another person and claims too contextual or uncertain for responsible correction. All observed classifications completed normally below the new cap, and no unwanted audio played. This provides a real multi-turn restraint soak while the assistant remained ready.
- The wearer then confirmed the glasses camera light was off after the background one-shot capture. This physically passes the post-capture idle-light gate; locked-screen capture remains separate.
- That conversation also found one false positive: “Can you tell him Redrum?” was classified as a direct request. Pronoun-only relay requests now fail closed locally as `ambiguous human relay`, and the classifier policy explicitly requires them to stay silent. Named, channel-explicit requests such as “Text John on iMessage ...” are unaffected. The revised signed build passes 75/75 Swift tests, strict code-sign verification, installation, Ray-Ban microphone/speaker routing, signaling HTTP 201, and `session.updated`, with the camera still closed while idle.
- Photo/look/read commands now force exactly one `capture_glasses_view` tool call, so the voice model cannot acknowledge “Take a pic” without operating the camera. A debug-only request injected through the production attention gate opened the closed Meta camera, captured one 1440x1080 reading-profile frame in 11.30 seconds, stopped the camera, queued the image, and completed the visual response. The normal no-injection process was then relaunched and reconnected. The build passes 76/76 Swift tests, signed-device compilation, strict code-sign verification, install, Ray-Ban audio routing, signaling HTTP 201, and `session.updated`; locked-screen physical proof remains separate.
- Locked-screen physical proof now passes on the revised no-idle-camera build. A failed retry first revealed that HFP transcription could render “Take a pic” as `take a pick`; transcription context now biases the literal camera phrase, and the local gate accepts only the narrow `take a pick` homophone while keeping `take your pick` non-visual. After a signed install and normal launch, device state reported `passcodeRequired: true`, the app remained backgrounded, and the exact three-word request was classified `explicit visual action`. The forced tool opened the stopped Meta camera, captured a 1440x1080 reading-profile frame with 29 OCR characters at 1.000 confidence in 7.91 seconds, stopped the camera immediately, queued the image, and finished a 461-token spoken visual response in about 14.12 seconds end to end. The wearer confirmed both that the description was correct and that the white camera light was off afterward. The build passes 76/76 Swift tests, strict signature verification, physical installation, Ray-Ban microphone/speaker routing, signaling HTTP `201`, and `session.updated`.
- The first response-serialization and VAD-interruption repair did not pass physical acceptance: the wearer still heard the Apollo correction stop before its final date. The latest build also raises the coach-only Realtime allowance from 100 to 512 output tokens, retains the classifier's 25-word content limit, and records `response.done` status, reason, and output-token usage. A debug-only injected Apollo transcript then ran through the real classifier and audio response: classification completed at 91 output tokens, selected `coach_interjection`, and the follow-up completed at 164 output tokens with `status=completed` and no error/reason. The old 100-token cap was therefore objectively too small. The wearer confirmed the automatic correction finished, although the ending sounded close to a cutoff. The exact Apollo physical playback gate now passes with measured token headroom; broader unseeded fact-check quality remains a separate soak concern.
- The immediate locked-phone regression gate passes: no touch was required, the camera activated only for the explicit request, the spoken visual answer was correct, and the white camera light turned off afterward.

## 2026-08-26 — physical iPhone integration build

Current deployment target:

- App: `com.richardholguin.CodexLensApp`
- Device: physical iPhone 17 Pro
- Signing: Apple Development, automatic development provisioning
- Mac gateway: LaunchAgent `com.codex-lens.gateway`, loopback only
- Remote path: Tailscale Serve, tailnet only, to `127.0.0.1:8787`
- Meta device: Ray-Ban Meta `0030`, DAT SDK 0.9

Verified in this build:

- Full `npm run verify` passes under the required Node 26 runtime: 52 gateway test files and 447 tests.
- `swift test` passes 70 tests, including attention-gate, confirmation, reconnect, sequential fresh-credential, local-time-tool, fast frontmost-app lookup, text-authorization lifecycle, glasses-only audio-route policy, and visual-frame quality selection coverage.
- A signed physical-device Xcode build succeeds and installs on the iPhone.
- The phone verifies its bearer token against the protected project route before showing the gateway as connected.
- Realtime waits for OpenAI's `session.updated` event before marking voice live.
- A fresh physical-device launch uses one gateway credential and one OpenAI signaling request, receives HTTP `201`, then receives `session.updated` and marks voice connected. Foreground callbacks no longer start a duplicate peer while initial signaling is in flight.
- Signaling has a 20-second timeout, transient setup disconnects do not kill a valid negotiation, and every genuine reconnect mints a fresh single-use credential.
- The microphone track starts disabled and is enabled only after session configuration.
- Bluetooth HFP routing requires a connected Ray-Ban/Meta input. The assistant pauses and deactivates Realtime instead of falling back to the iPhone microphone.
- Realtime audio interruptions, media-service resets, and route changes trigger recovery.
- Realtime sessions renew before the 60-minute platform ceiling, with a 55-minute fallback rollover. Renewal drains active visual and tool calls first so an old `call_id` cannot be delivered into the replacement session.
- Meta background capture uses the DAT-supported two-frame-per-second HVC1 configuration and adapts capture work to the phone's thermal state.
- Meta session, stream, device-link, and stale-callback lifecycles are identity-bound and recover automatically.
- Ordinary spoken photo/look requests now use one lower-latency frame after the locked `Take a pic` run measured 13.69 seconds on the high-resolution reading profile. Explicit text, screen, code, page, sign, or document reading still uses two serial high-resolution candidates; explicit careful mode uses three. Apple Vision OCR character, line, confidence, and sharpness metrics select one best original frame; only its metadata-stripped JPEG and OCR are sent. Live device diagnostics reported thermal state `none` during the verified capture runs. The faster ordinary-photo policy still requires a locked physical timing and correctness run.
- Native Mac Computer Use completed harmless live acceptance actions in VS Code and against visible Chrome UI, with the resulting UI state verified.
- A dedicated read-only frontmost-app path returns the active Mac app without a screenshot or Computer Use model turn. The signed physical-iPhone build installed and launched, the Realtime model selected the new tool from a natural glasses request, the authenticated gateway returned `ChatGPT` in 0.26 seconds, and the wearer confirmed the spoken answer was correct and fast both unlocked and after locking the phone without touching it.
- Ordinary reversible Mac and UI actions execute directly. Consequential actions require exact confirmation of the specific action, target, and content; confirmation records use an expiring digest, one-time consumption, and replay rejection.
- Explicit Chrome requests are not allowed to fall back to desktop clicks or Clawd Cursor.
- Official Chrome DOM control in the detached gateway worker failed 0/3 acceptance checks. The official in-app Browser also failed 0/3 isolated blank-tab checks. Neither browser capability is claimed as working in the gateway.
- Clawd Cursor is configured as a last-resort native-GUI fallback only.
- Conversation coaching is an always-available quiet behavior, not a wake-word mode. Undiarized ambient speech is treated conservatively.
- A physical debug acceptance run on the unlocked iPhone used a 20-second test-only rollover interval. The initial connection at 15:28:47 PDT and renewals at 15:29:09, 15:29:31, and 15:29:53 each minted exactly one fresh gateway credential, received signaling HTTP `201`, reached ICE connected and data-channel open, received `session.created` and `session.updated`, and returned to `session configured and connected`. No 401, 403, 503, timeout, `INVALID_STATE`, or retry storm appeared. The app was then relaunched without the test override; its production 55-minute timing is active.
- The final combined build repeated that physical acceptance after the lifecycle hardening. Starting at 15:55:13 PDT, connection generations 1 through 5 completed an initial session plus four consecutive 20-second renewals. Every generation received HTTP `201`, ICE connected, an open data channel, `session.created`, and `session.updated`; Ray-Ban microphone/speaker routing remained selected and camera recovery succeeded after each renewal. The app was then relaunched at 15:57:06 without the debug override, so production 55-minute timing is active.
- WebRTC mutable state and callbacks are serialized on one queue and tagged with connection generations. Stale peer, data-channel, SDP, response, transcript, tool, or disconnect callbacks cannot mutate the replacement session.
- Every local `response.create` is now reserved as active work before it is sent and correlated through `response.created` or a matching server error. Rollover also requires a 500 ms stable-idle window, closing the gap where a direct request or tool follow-up could be cut off just before OpenAI reported it active.
- Rollover drains are bounded by both a 20-second maximum and the server-expiry safety deadline. Safe read/camera/prepare work can be canceled at the deadline; consequential calls keep replay tombstones, are never automatically re-executed, and cannot attach stale results to a renewed session.
- Camera readiness now requires a connected, SDK-compatible device rather than treating any Bluetooth-connected Meta record as camera-eligible.
- The same combined install armed the Meta background camera and reported live thermal state `none`. Meta's documented temporary video-stream pause during still capture no longer triggers destructive camera recovery, and overlapping still requests are rejected.
- Failed initial gateway bootstrap now keeps the assistant armed and retries automatically instead of silently stopping.
- A locked-iPhone physical run kept app process 6586 alive after the side button lock and recovered the Ray-Ban external-accessory link, glasses microphone/speaker route, Meta SDK session, and background HVC1 camera stream without unlocking. Camera recovery succeeded after eight bounded attempts and continued to report thermal state `none`.
- During that locked run, the natural no-wake request `What time is it?` was classified as a direct request and reached the assistant. The model answered that it did not know the time, exposing a missing capability rather than a wake-word or background-audio failure.
- A later locked physical run on the current-time build entered `UIApplicationDidEnterBackground`, heard the same natural no-wake request, classified it `direct_request`, called the phone-local time capability, and spoke the correct answer through the Ray-Ban audio route. The wearer confirmed the answer was audible while the phone remained locked.
- A fold/reopen recovery run then closed the glasses, reopened them, heard startup, locked the iPhone, and asked the same no-wake time question without touching the phone. The assistant spoke the correct time through the glasses, proving locked voice recovery before the separate camera check below.
- The same post-fold locked session then accepted three consecutive natural photo requests without touching the phone. All three glasses captures completed and the wearer confirmed all three spoken descriptions were correct, with the first detailed. The current signed build therefore passed the 3-for-3 locked post-fold camera re-arm gate.
- One earlier spoken locked-phone photo request completed end to end: a 1080x1440 still was captured from the glasses, sanitized from 427,555 bytes to 128,380 bytes, and queued to the model in approximately 4.24 seconds. The later 3-for-3 post-fold run above supersedes that earlier one-capture correctness evidence; exact latency for the later run was not instrumented.
- A second locked physical capture on the later quality build returned a 1440x1080 still with on-device OCR and was confirmed accurate by the wearer. Its first usable frame completed in 10.39 seconds, but the forced second still timed out, making the total 22.58 seconds. That physical evidence drove the one-frame ordinary-look policy; the faster policy still needs a locked-device timing run.
- Ambiguous human-to-human questions observed while the phone was locked were classified `silent`, providing live evidence that the assistant can remain quiet. Fact-check/help interjection quality and explicit conversation-end reset remain separate acceptance gates.
- Live Conversation Coach restraint then passed an explicit ordinary-speech test: “I went to the store yesterday and bought some coffee” was classified `silent`, and the wearer confirmed no audio played. The first proactive fact-check test failed: “No, I’m sure Apollo 11 landed on the Moon in 1972” was classified `silent` because the classifier incorrectly reasoned that debate interjection was outside direct assistance. The classifier policy now makes proactive debate correction an independent core reason to speak, requires no direct request, and includes the exact Apollo `1972` to `1969` case as a mandatory interjection example. The revised build passes 71/71 Swift tests, signed physical-device build, strict signature verification, install, launch, Ray-Ban audio routing, and camera re-arm. On the repeat physical test, the runtime selected `coach_interjection`, and the wearer confirmed the glasses correctly interrupted with the factual correction. An unseeded correction and separate-speaker test remain required before claiming broad coaching reliability.
- The wearer tested with his son and confirmed the glasses reliably heard the wearer but not the other speaker at ordinary conversation distance. Meta's public iOS audio path is narrowband HFP beamformed toward the wearer; it does not expose raw room or microphone-array audio. A `far_field` experiment heard a loud nearby Mac voice but did not make the real son test reliable. The wearer accepted wearer-only factual correction as the release behavior, so production returned to `near_field` to optimize his speech. OpenAI's speaker-labeled Realtime transcription model was also tested, but the live project rejected it with `Your organization does not have access to this transcription model.` The production build retains supported `gpt-4o-mini-transcribe` and does not claim speaker identity.
- The wearer-focused `near_field` build passes 74/74 Swift tests, signed-device build, strict signature verification, install, and launch on the physical iPhone. The live app selected `RB Meta 0030` for microphone and sound, received signaling HTTP `201`, reached `session.updated`, and armed the camera on its first recovery attempt.
- Conversation context now expires on a real 90-second state-queue timer instead of waiting lazily for the next utterance. Each completed transcript rearms one bounded reset; explicit ending, disconnect, and newer-speech paths cancel stale work. The signed update installed and launched with Ray-Ban audio, `session.updated`, and first-attempt camera readiness. Locked physical timer evidence remains pending.
- A later accelerated acceptance run entered `UIApplicationDidEnterBackground` before connection generation 2. Generations 2 through 9 then completed eight consecutive locked 20-second renewals; each received signaling HTTP `201`, reached ICE connected and data-channel open, received `session.created` and `session.updated`, retained the Ray-Ban microphone/speaker route, and completed camera recovery. After the renewals, the natural no-wake request `What time is it?` was classified `direct_request`, and the wearer confirmed the spoken answer was correct with date and time-zone detail. No retry storm or connection failure appeared. The app was then relaunched without `CODEX_LENS_ROLLOVER_SECONDS`; the fresh production session connected, armed the Ray-Ban audio route and camera, reported thermal state `none`, and emitted no debug rollover schedule.

Latest signed build installed and launched:

- The installed fold/reopen repair keeps the existing configured WebRTC peer and audio unit alive while the Ray-Ban route is absent, but disables the outgoing audio track so the temporary iPhone-microphone fallback is not transmitted. In a true locked-phone physical run, folding produced `transmit=false keepalive=true`; reopening restored `Mic: RB Meta 0030 · Sound: RB Meta 0030`, `transmit=true`, and advancing outbound packet/byte counters without renegotiating a replacement peer.
- After that locked reopen, the wearer received a complete answer to a natural current-time request and then triggered `Take a pic` without a wake word or tap. The camera opened on demand, returned one 1440x1080 frame in 13.69 seconds, shut down, and completed the spoken visual result. The wearer confirmed the result worked and the white camera light was off afterward. Correctness, locked reconnect, and camera shutdown pass; the measured 13.69-second capture delay remains open.

- Screen-lock camera lifecycle work is serialized. If the app moves to the background during a still capture or camera recovery, it defers the background camera preparation and rearms after the visual request finishes instead of starting overlapping Meta SDK lifecycle work.
- Realtime start, recovery, and rollover use a bounded iOS background-execution bridge so screen lock cannot suspend the app during the gap between the old WebRTC audio track and the replacement peer connection.
- The phone now exposes a local `get_current_time` tool containing live local date, time, time zone, UTC offset, and ISO timestamp. The Realtime instructions require that tool for current-time questions.
- Text confirmation now becomes valid only after the app observes the assistant's completed spoken readback. Pre-readback confirmation, `send it`, stale or expired authorization, unrelated intervening speech, reconnect, and rollover all fail closed and invalidate the pending action.
- Ambient texting phrases such as `Text me when you arrive` no longer bypass the quiet intent classifier.
- Message previews bind the exact requested service (`iMessage`, `SMS`, or `RCS`), enabled and connected Messages account, destination, body, expiry, and digest. The sender never silently falls back to another service. A persistent SQLite intent ledger records `prepared`, `sending`, `accepted`, `uncertain`, `expired`, or `rejected` and prevents a gateway restart from replaying an accepted or uncertain send. Authenticated readiness and redacted status endpoints are deployed; no real message has been sent.
- Live Messages readiness was repaired after macOS exposed three account records whose `service type` property throws AppleEvent error `-10000`. The gateway now skips unreadable records and still requires an exact enabled, connected service. The authenticated live result reports Contacts accessible, one iMessage account, one SMS account, no RCS account, and `ready=true`; no message was sent.
- A later live check found readiness depended on Contacts already running: the allowed gateway process received AppleScript application error `-600` when Contacts was closed. The deployed gateway now launches Contacts before readiness or name lookup and launches Messages before account inspection or send. With Contacts fully stopped, the rebuilt and restarted gateway launched it and returned `ready=true` with one iMessage and one SMS account. Node 26 typecheck, lint, and 447/447 tests pass; no message was sent.
- The production gateway now actually opens `CODEX_LENS_DB_PATH`; its live SQLite database contains the new `message_intents` table. The prior entrypoint ignored that configured path and used memory-only storage.
- The prior installed build exposed a privacy and battery defect while the glasses were charging: after the glasses HFP route disappeared, WebRTC reconnected through the iPhone microphone and receiver. That old process was terminated. The new build fails closed: its microphone track can enable only when Ray-Ban/Meta HFP is the active input, route loss tears down and deactivates Realtime, passive monitoring does not activate the iPhone microphone, and the armed assistant reconnects only after the glasses audio input returns.
- This combined build passes 70/70 Swift tests, `git diff --check`, a signed physical-iPhone Xcode build, strict code-sign verification, gateway typecheck, gateway lint, and 447/447 gateway tests under Node 26.
- The final latency/rollover build installed at 18:00 PDT and launched on the physical iPhone. A fresh normal launch selected `Mic: RB Meta 0030 · Sound: RB Meta 0030`, received signaling HTTP `201`, opened the data channel, received `session.created` and `session.updated`, armed the high-resolution HVC1 background camera, and reported thermal state `none`.
- The first accelerated run exposed a timer-reset race: repeated healthy audio-route callbacks continually pushed the debug rollover deadline forward. The final build retains the earliest scheduled deadline. In the repeat run, generations 1 through 5 completed an initial connection plus four consecutive renewals at roughly 22-second intervals. Every generation received HTTP `201`, opened the data channel, received `session.created` and `session.updated`, retained Ray-Ban audio, and rearmed the camera. The app was then relaunched without `CODEX_LENS_ROLLOVER_SECONDS`; no debug scheduling line appeared and normal production timing is active.

Still required before calling locked-phone operation proven:

- The 3-for-3 locked post-fold camera re-arm gate now passes. Exact one-frame latency was not instrumented in this wearer-confirmation run, so performance timing remains distinct from correctness and recovery.
- Locked rollover now passes eight consecutive accelerated renewals plus a correct post-renewal spoken time answer. The no-override relaunch also passes, restoring normal 55-minute production timing.
- Wearer factual correction and ordinary-conversation silence pass physically. Reliable correction of another person is not a release gate because the public Ray-Ban HFP route does not reliably capture distant speech. Still physically exercise an explicit conversation ending and a 90-second quiet-gap reset.
- Run a longer locked-phone soak. iOS cannot guarantee survival after force quit, reboot, or operating-system termination; the app must be opened again in those cases.
- Browser automation remains unavailable in the detached gateway worker. Native Computer Use can operate visible Chrome UI, but that is not official Chrome DOM control and must not be represented as such.
- Messages must not be considered release-ready until Contacts and Messages permissions are granted to the actual gateway Node executable, recipient and service ambiguity are resolved, and one-message delivery plus replay tests pass on the actual Mac account. iMessage and SMS/RCS require separate proof.

Build commands:

```sh
PATH=/opt/homebrew/opt/node@26/bin:/opt/homebrew/bin:$PATH npm run verify
swift test --package-path apps/ios/CodexLensKit
xcodebuild \
  -project apps/ios/CodexLensApp/CodexLensApp.xcodeproj \
  -scheme CodexLensApp \
  -configuration Debug \
  -destination 'id=AC3C5D6B-879A-5C78-98A5-EA3EEDE736FD' \
  -derivedDataPath /tmp/codex-lens-photo-quality-derived \
  build
```
