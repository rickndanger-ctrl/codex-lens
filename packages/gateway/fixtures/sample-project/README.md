# Sample Project

This fixture is the gateway registry's only seeded allowlisted project. It is safe for workspace-write tests and does not permit dependency installation, commits, pushes, or deployments.
